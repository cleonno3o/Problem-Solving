#include <iostream>
using namespace std;
int n, m;
int arr[11];
void DFS(int l)
{
    
}
int main()
{
    cin >> n >> m;
    for (int i = 1; i <= n; i++)
    {
        cin >> arr[i];
    }
    DFS(1);
    return 0;
}